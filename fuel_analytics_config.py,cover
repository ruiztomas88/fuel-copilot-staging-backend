> """
> Centralized Configuration - Replaces hardcoded magic numbers
> ═══════════════════════════════════════════════════════════════════════════════
> v6.2.2 - All thresholds and constants in one place
> Environment variables override defaults
> """
  
! import os
  
  # THEFT DETECTION
! MIN_THEFT_GALLONS = float(os.getenv("MIN_THEFT_GALLONS", "10.0"))
! CONFIRMED_THEFT_THRESHOLD = 85.0
! SUSPICIOUS_THEFT_THRESHOLD = 60.0
  
  # MPG THRESHOLDS
! TARGET_MPG = 6.5
! WARNING_MPG = 5.5
! CRITICAL_MPG = 5.0
  
  # ENGINE HEALTH
! OIL_PRESSURE_CRITICAL_LOW = 20.0
! OIL_PRESSURE_WARNING_LOW = 30.0
! COOLANT_TEMP_CRITICAL_HIGH = 230.0
! COOLANT_TEMP_WARNING_HIGH = 220.0
! BATTERY_VOLTAGE_CRITICAL_LOW = 11.8
! BATTERY_VOLTAGE_WARNING_LOW = 12.2
  
  # UTILIZATION
! ELITE_UTILIZATION_PCT = 95.0
! OPTIMAL_UTILIZATION_PCT = 85.0
! MIN_MOVING_SPEED_MPH = 5.0
  
  # GEOFENCE
! DEFAULT_GEOFENCE_RADIUS_METERS = 500
! MAX_GEOFENCE_SEARCH_RADIUS_KM = 1.0
  
  # CIRCUIT BREAKER
! CIRCUIT_FAILURE_THRESHOLD = 5
! CIRCUIT_SUCCESS_THRESHOLD = 2
! CIRCUIT_TIMEOUT_SECONDS = 60
  
  # PREDICTIVE MAINTENANCE
! MIN_HISTORY_FOR_PREDICTION = 3
! MIN_READINGS_PER_DAY = 0.1
! MAX_READINGS_PER_DAY = 10000.0
! DEFAULT_READINGS_PER_DAY = 1.0
